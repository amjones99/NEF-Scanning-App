%h1 QR Code
%table
  %thead
    %tr
      %th Booking Reference

  %tbody
    - @bookings.each do |b|
      %tr
        %td= b.id.to_s

%br

- @bookings.each do |b|
  %img{src: generate_qr(b.id.to_s), class: "qr-code"}

= link_to 'Back', scans_path
