%h1 Account
%p#notice= notice

%p
  %b Username:
  = current_user.username
%p
  %b Email:
  = current_user.email
%p
  %b Name:
  = current_user.name

= link_to "Request Certificate of attendance", request_certificate_booking_path(@booking), method: :put, :class => "btn btn-info"
= link_to "Cancel Request", cancel_certificate_booking_path(@booking), method: :put, :class => "btn btn-info"

%br
%br
= link_to 'Edit Account', "/users/editU", :class => "btn btn-primary"
= link_to 'Change Password', "/users/changepw", :class => "btn btn-info"
