%h2 All users

.table-responsive
%table.table.table-striped
  %thead
    %tr
      %th= sortable 'Username'
      %th= sortable 'access', 'Access'
      %th= sortable 'institution', 'Institution'
      %th= sortable 'email', 'Email'
      %th= sortable 'name', 'Name'
      %th= sortable 'booking_reference', 'Booking Ref'
      %th= sortable 'attended', 'Attended'
      %th
      %th
      %th

  %tbody
    - @users_with_attended.each do |user|
      %tr
        %td= user.username
        %td= user.access
        %td= user.institution
        %td= user.email
        %td= user.name
        %td= user.booking_reference
        %td= user.bookings_attended
        %td= link_to 'Show', user
        %td= link_to 'Edit', edit_user_path(user)
        %td= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }

%br


= link_to 'New User', new_user_path
