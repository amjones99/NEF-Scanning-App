%h3 Notifications
.table-responsive
%table.table.table-striped
  %thead
    %tr
      %th= sortable 'not_id', 'Notification Name'
      %th= sortable 'not_des', 'Notification Description'
      %th= sortable 'time', 'Time'
      %th
      %th
      %th

  %tbody
    - @notifications.each do |notification|
      %tr
        %td= notification.not_id
        %td= notification.not_des
        %td= notification.time

%br